<section style="padding: 40px 24px 100px; text-align: center;">

  <!-- Label — Tag de Luxo minimalista -->
  <span
    style="
      font-family: var(--font-body);
      font-size: 0.625rem;
      font-weight: 600;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: var(--color-text);
      border: 1px solid rgba(203, 180, 165, 0.5);
      background-color: rgba(255, 255, 255, 0.45);
      padding: 0.4rem 1.1rem;
      border-radius: 2px;
      display: inline-block;
      box-shadow: 0 1px 2px rgba(0, 0, 0, 0.02);
    "
  >
    Agende seu horário
  </span>

  <!-- Quote editorial: Libertinus Serif Display -->
  <p
    style="
      font-family: var(--font-heading);
      font-size: clamp(1.35rem, 2.8vw, 1.9rem);
      font-weight: 400;
      line-height: 1.4;
      letter-spacing: -0.01em;
      color: var(--color-text);
      max-width: 32rem;
      margin: 1.75rem auto 0;
    "
  >
    Um refúgio de sofisticação projetado para proporcionar uma experiência única.
  </p>

  <!-- Botões Manrope -->
  <div class="mt-10 flex flex-wrap items-center justify-center gap-4">
    <a href="#contato" class="btn btn--primary">
      <span>Verificar Disponibilidade</span>
    </a>
  </div>
</section>
