<section id="sobre" class="bg-[var(--color-beige-light)]" style="min-height: 50vh; display: flex; align-items: center;">
  <div class="max-w-3xl mx-auto px-6 text-center py-16">

    <!-- Label — Tag de Luxo minimalista -->
    <span
      style="
        font-family: var(--font-body);
        font-size: 0.625rem;
        font-weight: 600;
        letter-spacing: 0.22em;
        text-transform: uppercase;
        color: var(--color-text);
        border: 1px solid rgba(203, 180, 165, 0.5);
        background-color: rgba(255, 255, 255, 0.45);
        padding: 0.4rem 1.1rem;
        border-radius: 2px;
        display: inline-block;
        box-shadow: 0 1px 2px rgba(0, 0, 0, 0.02);
      "
    >
      Mais de 10 anos de experiência
    </span>

    <!-- Citação editorial — Libertinus Serif Display -->
    <p
      style="
        font-family: var(--font-heading);
        font-size: clamp(1.25rem, 2.5vw, 1.75rem);
        font-weight: 400;
        line-height: 1.5;
        letter-spacing: -0.01em;
        color: var(--color-text);
        margin-top: 1.5rem;
      "
    >
      Dedicando exclusivamente à saúde e estética das suas unhas. Utilizo as técnicas mais refinadas para preservar sua integridade natural, transformando o cuidado em um ritual de bem-estar.
    </p>
  </div>
</section>
